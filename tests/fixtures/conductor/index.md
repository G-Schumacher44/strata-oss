# Conductor Index — test fixture

Deterministic fixture for `test_strata_conductor_status`. Independent of the
repo's live `conductor/` so the test passes in both the private repo and the
sanitized public mirror (where live working state is stripped by `.publicignore`).

Active slice: slice-99-test-fixture

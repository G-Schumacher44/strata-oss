# Input: lookml_view_reviewer

**Context:** `semantic_layer_audit` flagged `finance_orders` as HIGH — 9 drift hits,
backs the live `finance_overview` explore.

```
view: finance_orders
source_file: (derive from IR)
```

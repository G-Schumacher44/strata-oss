[project]
name = "strata-mcpb-shim"
version = "0.1.6"
description = "MCP Bundle launcher for Strata — resolves the published strata-lookml package via uv."
requires-python = ">=3.11"
dependencies = [
  "strata-lookml==0.1.6",
]
